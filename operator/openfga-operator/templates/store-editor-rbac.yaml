apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: operator-store-editor-role
  labels:
  {{- include "openfga-operator.labels" . | nindent 4 }}
rules:
- apiGroups:
  - extensions.openfga-controller
  resources:
  - stores
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - extensions.openfga-controller
  resources:
  - stores/status
  verbs:
  - get