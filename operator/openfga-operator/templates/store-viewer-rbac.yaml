apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: operator-store-viewer-role
  labels:
  {{- include "openfga-operator.labels" . | nindent 4 }}
rules:
- apiGroups:
  - extensions.openfga-controller
  resources:
  - stores
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - extensions.openfga-controller
  resources:
  - stores/status
  verbs:
  - get